const clientTypes = `
  # Client definition types
  type Client {
    id: ID!
    name: String
    created_at: String!
    updated_at: String!
  }
`;

const clientQueries = ``;

const clientMutations = ``;

export { clientTypes, clientQueries, clientMutations }
